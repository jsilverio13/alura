#_{:clj-kondo/ignore [:refer-all]}
(ns datomic-bindings-transaction-functions-e-filters.core-test
  (:require [clojure.test :refer [deftest testing is]]
            [datomic-bindings-transaction-functions-e-filters.core :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
